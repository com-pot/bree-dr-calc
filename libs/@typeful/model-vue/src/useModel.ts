import ModelAccessor from "@typeful/model/ModelAccessor";

export default function useModel(modelName: string): ModelAccessor {
  throw new Error("use model not implemented")
  // return new ModelAccessor()
}
